require('dotenv').config(); const { createClient } = require('@supabase/supabase-js'); const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient({ datasources: { db: { url: 'postgresql://postgres.ktnqomkuuisfafoaxwtc:Istuti98510@aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true' } } }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function main() { const email = 'istuti1133@gmail.com'; const password = 'Istuti1133'; console.log('Creating user: ' + email); let { data, error } = await supabase.auth.signUp({ email, password }); if (error && error.message !== 'User already registered') { console.error('Signup error:', error); return; } const authUsers = await prisma.$queryRawUnsafe("SELECT id FROM auth.users WHERE email = 'istuti1133@gmail.com'"); if (!authUsers || authUsers.length === 0) { console.error('User not found'); return; } const userId = authUsers[0].id; console.log('User ID found: ' + userId); await prisma.$executeRawUnsafe("UPDATE auth.users SET email_confirmed_at = NOW() WHERE id = '" + userId + "'"); console.log('Email auto-confirmed.'); const existingUser = await prisma.user.findUnique({ where: { id: userId } }); if (existingUser) { await prisma.user.update({ where: { id: userId }, data: { role: 'SUPER_ADMIN' } }); console.log('User updated to SUPER_ADMIN.'); } else { await prisma.user.create({ data: { id: userId, email, role: 'SUPER_ADMIN' } }); console.log('User created in public.User as SUPER_ADMIN.'); } console.log('Process complete! User can now log in.'); } main().catch(console.error).finally(() => prisma.$disconnect());
